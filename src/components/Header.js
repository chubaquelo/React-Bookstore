import React from 'react';

const Header = () => (
  <Header className="sticky w-screen h-10 bg-white text-blue-500 flex flex-row align-middle">
    <h1>BookStore</h1>
  </Header>
);

export default Header;
